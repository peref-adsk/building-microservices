version: "3.7"
services:
  web:
    container_name: carts
    build:
      context: .
      dockerfile: Dockerfile.dev
    environment:
      - PORT=9200
    ports:
      - 9200:9200
    volumes:
      - .:/app-carts